import type { CSSProperties } from "react";

export const styles = {
  empty: {
    padding: 40,
  },
  label: {
    fontSize: 12,
    fontWeight: 700,
    fill: "#1e63d6",
  },
} satisfies Record<string, CSSProperties>;
